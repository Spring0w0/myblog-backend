# 主配置文件
spring:
  profiles:
    active: dev  # 默认使用开发环境

# JWT配置
jwt:
  secret: MizukiSpring0w0BlogSecretKey2024!  # JWT密钥，至少32字符，生产环境请修改
  expiration: 7200  # JWT过期时间，单位秒

# 日志配置
logging:
  level:
    com:
      spring0w0:
        myblog: DEBUG